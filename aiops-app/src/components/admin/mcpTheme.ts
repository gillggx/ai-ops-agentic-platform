/**
 * Back-compat shim. The design tokens moved to the shared
 * {@link ../common/uiTokens} module when DataResultView was promoted out of
 * the admin folder for site-wide reuse. Existing admin imports keep working.
 */
export * from "@/components/common/uiTokens";
