/**
 * /chatops — ChatOps presentation (Phase B, design v2).
 *
 * The page itself is empty on purpose: AppShell owns the layout for this
 * route (conversation sidebar + the ONE shared AIAgentPanel rendered as a
 * centered wide column), so the Live Canvas overlay and all glass-event
 * wiring keep working without a second panel instance.
 */
export default function ChatOpsPage() {
  return null;
}
