"""Agent eval harness — see runner.py + scorers.py.

Run from repo root:

    python -m tests.agent_eval.runner --suite all
    python -m tests.agent_eval.runner --suite builder_intent_7bucket
    python -m tests.agent_eval.runner --update-baseline
"""
