__pycache__
*.pyc

# SQLite / database files — never commit local DBs
*.db
*.sqlite3

# Environment files — each environment keeps its own .env
.env
.env.local
.env.*.local

# OntologySimulator frontend build artifacts
ontology_simulator/frontend/node_modules/
ontology_simulator/frontend/.next/
ontology_simulator/frontend/tsconfig.tsbuildinfo

# Backup files
*_backup*
*.local_backup.nginx_domain
