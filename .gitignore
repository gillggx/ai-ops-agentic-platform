# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
# NOTE: don't blanket-ignore `build/` — Next.js API route dirs can legitimately
# be named "build" (e.g. /api/agent/build). We only want to ignore Python
# package build outputs at the repo root.
/build/
/fastapi_backend_service/build/

# ── Virtual environments ──────────────────────────────────────────────────────
venv/
.venv/
env/

# ── Environment / secrets ─────────────────────────────────────────────────────
.env
.env.local
.env.*.local

# ── Database files ────────────────────────────────────────────────────────────
*.db
*.sqlite3
*_backup*
*.local_backup*
*.pre-pg-migration

# ── Migration dumps (one-shot SQLite→Postgres) ──────────────────────────
fastapi_backend_service/scripts/migration/dumps/*.jsonl

# ── Logs ──────────────────────────────────────────────────────────────────────
logs/
*.log

# ── Test / coverage ───────────────────────────────────────────────────────────
.coverage
.pytest_cache/
htmlcov/
test_outputs/

# ── Node / Next.js ────────────────────────────────────────────────────────────
node_modules/
.next/
out/
tsconfig.tsbuildinfo

# ── Java / Maven ─────────────────────────────────────────────────────────────
# Maven output dirs (replaces Gradle build/ as of 2026-05-11).
java-backend/target/
java-scheduler/target/
# Old Gradle leftovers — keep ignoring in case anyone pulls a stale branch.
java-backend/.gradle/
java-backend/build/
java-scheduler/build/
java-backend/bin/
java-backend/out/
java-backend/.idea/
java-scheduler/.idea/
*.class

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store

# ── IDE ───────────────────────────────────────────────────────────────────────
.idea/
.vscode/

# ── Playwright / E2E artifacts ────────────────────────────────────────────────
aiops-app/playwright-report/
aiops-app/test-results/
aiops-app/playwright/.cache/

# ── Local testing screenshots ────────────────────────────────────────────────
screenshots/

# Agent eval per-run reports (kept gitignored; baseline.json IS committed)
tests/agent_eval/reports/
tests/agent_eval/__pycache__/
